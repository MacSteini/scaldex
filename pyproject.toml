[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "scaldex"
version = "0.1.0"
description = "Reproducible Codex AGENTS.md measurement harness"
requires-python = ">=3.11"
dependencies = []

[project.scripts]
scaldex = "scaldex.cli:main"

[tool.setuptools.packages.find]
where = ["src"]
